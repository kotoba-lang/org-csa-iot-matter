{
  "name": "org-csa-iot-matter",
  "private": true,
  "description": "Matter message frame + TLV codec in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
